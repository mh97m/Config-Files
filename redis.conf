# Redis configuration file

# Specify the port number
port 6379

# Specify the bind address (optional)
# bind 127.0.0.1

# Specify the password (optional)
# requirepass your_redis_password_here

# Specify the database directory (optional)
# dir /path/to/database/directory

# Add any other desired configuration options
